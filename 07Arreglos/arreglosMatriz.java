import java.util.Scanner;



public class arreglosMatriz {
    public static void main(String[]args){
     

    //vamos a realizar un arreglo unidimensional y correrlo para ver que hay dentro de cada elemnto
    int arreglo[] = new int[10]
    //vamos a recorrerlo
    Scanner entrada = new Scanner
    (System.in);
    for(int j = 0; j < arrreglo.length; j++){
        System.out.println(Ingresa el valor de la posición del arreglo");
        valor = entrada.nextInt();
    }
    for(int i = 0; i < arrglo.length; i++){
        System.out.println("La magia:"+ arreglo[i] );
        //for unicamente para numeros, for itch para cadenas y objetos
    }
}
}